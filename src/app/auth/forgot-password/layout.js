export const metadata = {
  title: 'Reset Password - LomeyoLabs',
  description: 'Reset your LomeyoLabs account password.',
  robots: {
    index: false,
    follow: false,
  }
}

export default function Layout({ children }) {
  return children
} 